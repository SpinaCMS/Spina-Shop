$("<%=j render partial: 'customer', collection: @customers.to_a.uniq %>").appendTo($(".infinite-table table tbody"));

// Update pagination link
$('.pagination').html("<%=j link_to_next_page @customers, 'Next', params: params.except(*request.path_parameters.keys).permit! %>").infiniteScroll();
