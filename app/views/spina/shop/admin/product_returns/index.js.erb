$("<%=j render partial: 'product_return', collection: @product_returns %>").appendTo($(".infinite-table > table > tbody"));

// Update pagination link
$('.pagination').html("<%=j link_to 'Next', spina.shop_admin_product_returns_path(page: [params[:page].to_i, 1].max + 1), rel: 'next' %>").infiniteScroll();
