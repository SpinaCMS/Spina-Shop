$("<%=j render partial: 'shop_review', collection: @shop_reviews %>").appendTo($(".infinite-table table tbody"));

// Update pagination link
$('.pagination').html("<%=j link_to 'Next', spina.shop_admin_shop_reviews_path(page: [params[:page].to_i, 1].max + 1), rel: 'next' %>").infiniteScroll();
