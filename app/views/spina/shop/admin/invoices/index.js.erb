$("<%=j render partial: 'invoice', collection: @invoices %>").appendTo($(".infinite-table > table > tbody"));

// Update pagination link
$('.pagination').html("<%=j link_to 'Next', spina.shop_admin_invoices_path(page: [params[:page].to_i, 1].max + 1), rel: 'next' %>").infiniteScroll();
