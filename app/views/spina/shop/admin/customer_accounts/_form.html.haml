= form_for @customer_account, url: @customer_account.new_record? ? spina.shop_admin_customer_customer_account_path(@customer) : spina.shop_admin_customer_customer_account_path(@customer, @customer_account) do |f|
  %header#header
    #header_actions
      = button_tag type: :submit, class: 'button button-primary' do
        = icon('check')
        = t('spina.shop.customer_accounts.save')

    .breadcrumbs= render_breadcrumbs separator: '<div class="divider"></div>'

  .sidebar-form
    .sidebar-form-content
      .well
        .sidebar-form-group
          .sidebar-form-label
            = Spina::Shop::CustomerAccount.human_attribute_name :email

          .sidebar-form-control
            = f.text_field :email, placeholder: Spina::Shop::Customer.human_attribute_name(:email)

        .sidebar-form-group
          .sidebar-form-label
            = Spina::Shop::CustomerAccount.human_attribute_name :password

          .sidebar-form-control
            = f.password_field :password, placeholder: Spina::Shop::CustomerAccount.human_attribute_name(:password)

        .sidebar-form-group
          .sidebar-form-label
            = Spina::Shop::CustomerAccount.human_attribute_name :password_confirmation

          .sidebar-form-control
            = f.password_field :password_confirmation, placeholder: Spina::Shop::CustomerAccount.human_attribute_name(:password_confirmation)