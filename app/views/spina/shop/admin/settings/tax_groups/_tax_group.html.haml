.table-container
  %table.table
    %tr
      %td
        %strong= tax_group.name
      %td.align-right.text-muted
        = tax_group.product_items.count
        products

    - tax_group.tax_rates.each do |tax_rate|
      %tr
        %td= tax_rate.label
        %td.align-right
          = "#{tax_rate.rate}%"
          %small= "BTW-code: #{tax_rate.code}"
