$("<%=j render partial: 'customer', collection: @customers.to_a.uniq %>").appendTo($(".infinite-table table tbody"));

// Update pagination link
$('.pagination').html("<%=j link_to 'Next', spina.shop_admin_customers_path(page: [params[:page].to_i, 1].max + 1), rel: 'next' %>").infiniteScroll();
