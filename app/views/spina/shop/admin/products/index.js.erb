$("<%=j render partial: 'single_product', collection: @products.to_a.uniq %>").appendTo($(".infinite-table > table > tbody"));

// Update pagination link
$('.pagination').html("<%=j link_to 'Next', spina.shop_admin_products_path(page: [params[:page].to_i, 1].max + 1), rel: 'next' %>").infiniteScroll();
