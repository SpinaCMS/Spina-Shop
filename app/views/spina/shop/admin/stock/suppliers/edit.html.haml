= render 'form'

.pull-right= link_to t('spina.delete'), spina.shop_admin_supplier_path(@supplier), class: 'button button-link button-danger', data: {turbo_method: :delete, turbo_confirm: t('spina.delete_confirmation', subject: @supplier.name)}