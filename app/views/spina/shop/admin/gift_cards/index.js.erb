$("<%=j render partial: 'gift_card', collection: @gift_cards %>").appendTo($(".infinite-table table tbody"));

// Update pagination link
$('.pagination').html("<%=j link_to 'Next', spina.shop_admin_gift_cards_path(page: [params[:page].to_i, 1].max + 1), rel: 'next' %>").infiniteScroll();
